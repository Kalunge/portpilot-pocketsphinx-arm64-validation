diff --git a/CMakeLists.txt b/CMakeLists.txt
index d824cbda..c9647a17 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -16,7 +16,9 @@ set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
 
 if(CMAKE_PROJECT_NAME STREQUAL PROJECT_NAME)
   include(CTest)
-  add_custom_target(check COMMAND ${CMAKE_CTEST_COMMAND})
+  add_custom_target(
+    check COMMAND ${CMAKE_CTEST_COMMAND} --build-config $<CONFIG>
+    )
 endif()
 
 include(CheckTypeSize)
diff --git a/test/unit/test_config.c b/test/unit/test_config.c
index 1a69baec..f7622096 100644
--- a/test/unit/test_config.c
+++ b/test/unit/test_config.c
@@ -2,6 +2,10 @@
 #include "pocketsphinx_internal.h"
 #include "test_macros.h"
 
+#ifdef _WIN32
+#define setenv(name, value, overwrite) _putenv_s(name, value)
+#endif
+
 #define LEN(array) (sizeof(array) / sizeof(array[0]))
 static char *good_argv[] = {
     "pocketsphinx",
diff --git a/test/unit/test_endpointer.c b/test/unit/test_endpointer.c
index 64f3c8cb..11c4be67 100644
--- a/test/unit/test_endpointer.c
+++ b/test/unit/test_endpointer.c
@@ -8,6 +8,11 @@
 #include "util/ckd_alloc.h"
 #include "test_macros.h"
 
+#ifdef _WIN32
+#define popen _popen
+#define pclose _pclose
+#endif
+
 static int sample_rates[] = {
     8000,
     16000,
diff --git a/test/unit/test_vad.c b/test/unit/test_vad.c
index b85cfb16..f2b06864 100644
--- a/test/unit/test_vad.c
+++ b/test/unit/test_vad.c
@@ -9,6 +9,11 @@
 #include "util/ckd_alloc.h"
 #include "test_macros.h"
 
+#ifdef _WIN32
+#define popen _popen
+#define pclose _pclose
+#endif
+
 static const char *expecteds[] = {
     "011110111111111111111111111100",
     "011110111111111111111111111100",
